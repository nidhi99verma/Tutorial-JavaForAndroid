package com.example.tut2;

public interface CarInterface17 {
     void start();
     void move (int speed);
}
//Abstract method: abstract method can not heave body for abstract method
//Interface: inside can not have private or protected Abstract method